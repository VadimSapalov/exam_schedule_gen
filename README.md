cd backend
При інсталяції потрібно запустити pip install -r requirements.txt
uvicorn main:app --reload
в другому терміналі cd frontend
npm run dev
та в файлі database.py вказати посилання на бд
сайт працює на localhost:5173
